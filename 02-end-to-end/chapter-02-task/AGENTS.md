# AI Agents Guide for `02-coding-interview`

## How we work
- We use **uv** for Python dependency management.
- We use **ruff** for linting.
- We use **conventional commits**.

## Repositories
- `backend/`: FastAPI + Socket.IO
- `frontend/`: React + Vite

## Git Instructions
- ALWAYS commit after a logical step.
- Message format: `feat(scope): description` or `fix(scope): description`.
